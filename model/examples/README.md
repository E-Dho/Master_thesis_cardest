# Examples

Runtime scripts write small local artifacts here, such as
`synthetic_checkpoint.json`. Generated checkpoint JSON files are intentionally
ignored because they can be reproduced with `python3 -m model.scripts.train_synthetic`.

