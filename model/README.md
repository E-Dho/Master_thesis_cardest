# Model

Planned area for the learned cardinality-estimation model implementation.

This directory is intentionally minimal for now. Future code should keep model architecture, training loops, checkpoints metadata, and evaluation hooks separate from dataset and query generation utilities.
